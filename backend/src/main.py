from fastapi import FastAPI
from routers import user, products
import models.user 

app = FastAPI()

# We'll place the routes here later (Trabaho mo na to Pete)
# Our codes should be less than 300 para easy to maintain tas i-scale if bbenta natin
